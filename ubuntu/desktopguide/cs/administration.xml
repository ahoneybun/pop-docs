<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN" 
	"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
<!ENTITY % globalent SYSTEM "../../../libs/global.ent">
%globalent;
<!ENTITY % cdo-C SYSTEM "../../../libs/cdo-C.ent">
%cdo-C;
<!ENTITY % gnome-menus-C SYSTEM "../../../ubuntu/libs/gnome-menus-C.ent">
%gnome-menus-C;
<!ENTITY % xinclude SYSTEM "../../../libs/xinclude.mod">
%xinclude;
<!ENTITY language "en">
<!ENTITY ubuntu '<phrase>Ubuntu</phrase>'>
]>
<chapter id="root-and-sudo" status="review">
			<title>Administrative Tasks</title>
			<para>In Ubuntu, for security reasons, administrative tasks are confined to users with special privileges. Administrative access is given to individual users, who may use the <application>sudo</application> command to perform administrative tasks. The first user account you created on your system during installation will, by default, have access to sudo. You can restrict and enable sudo access to users with the <application>Users and Groups</application> application (see <xref linkend="users-and-groups"/> for more information).
			</para>
			<para>Pokud spustíte aplikaci, která vyžaduje administrátorská oprávnění, sudo vás požádá o vložení vašeho normálního uživatelského  hesla.  Tím je zajištěno, že škodlivé aplikace nemohou poškodit váš systém a slouží jako připomenutí toho, že provádíte úkoly správce systému, které vyžadují abyste byli pozorní a pečliví!</para>
			<para>Pro použití sudo v příkazové řádce jednoduše napište "sudo" před příkaz, který chcete spustit. Sudo vás pak požádá o vložení hesla.</para>
			<para>Sudo will remember your password for a set amount of time, which by default is 15 minutes, but can be changed. This feature was designed to allow users to perform multiple administrative tasks without being asked for a password each time.
			</para>
				   <note>
						<para>Pokud provádíte úkoly správce systému, buďte pečliví a pozorní, protože můžete systém poškodit !</para>
				   </note>
			<para>Další tipy pro používání sudo:</para>
			<itemizedlist>
				<listitem>
				<para>Poku chcete používat terminál s právy správce systému, napište v příkazové řádce "sudo -i".</para>
				</listitem>
				<listitem>
				<para>Veškeré standardní grafické konfigurační nástroje v Ubuntu již sudo využívají, a požádají vás o vložení vašeho hesla pokud to bude potřeba.</para>
				</listitem>
				<listitem>
				<para>For more information on the <application>sudo</application> program and the absence of a root user in Ubuntu, read the <ulink url="&wiki-RootSudo;">sudo page</ulink> on the Ubuntu wiki.
				</para>
				</listitem>

			</itemizedlist>
		</chapter>

