<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN" 
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
<!ENTITY % globalent SYSTEM "../../../libs/global.ent">
%globalent;
<!ENTITY % cdo-C SYSTEM "../../../libs/cdo-C.ent">
%cdo-C;
<!ENTITY % kde SYSTEM "../../libs/kde.ent">
%kde;
<!ENTITY % kde-menus-C SYSTEM "../../libs/kde-menus-C.ent">
%kde-menus-C;
<!ENTITY % kde-general SYSTEM
"/usr/share/apps/ksgmltools2/customization/obsolete/general.entities">
%kde-general;
<!ENTITY % xinclude SYSTEM "../../../libs/xinclude.mod">
%xinclude;
<!ENTITY language "en">
]>
<article id="hardware" status="complete">
<articleinfo>
<title>Hardware</title>
&klegalnotice;
<abstract>
<para>
This section contains information on the installation, configuration, and use
of hardware.
</para>
</abstract>
</articleinfo>

<!-- BLUETOOTH -->
<sect1 id="bluetooth" status="complete">
<title>Bluetooth</title>

<para>
More and more <trademark class="registered">Bluetooth</trademark> devices are
making their way into the market these days. Kubuntu has recognized this and
offers support for many <trademark class="registered">Bluetooth</trademark> 
devices. For <trademark class="registered">Bluetooth</trademark> configuration,
access the <guilabel>Bluetooth</guilabel> system settings by going to
&menusysset;. These settings provide you the options for configuring
<guilabel>Bluetooth Services</guilabel> as well as <guilabel>Paired Bluetooth
Devices</guilabel>.
</para>

<para>
<trademark class="registered">Bluetooth</trademark> Internet options are
available from within the <application>KMenu</application> by going to
&menubluechat; and &menublueobex;.
</para>

<para>
Further information concerning <trademark>Bluetooth</trademark> can be located
on the Ubuntu community documentation site at <ulink
url="https://help.ubuntu.com/community/BluetoothSetup"/>.
</para>

</sect1>

<!-- CAMERAS -->
<sect1 id="cameras" status="complete">
<title>Cameras</title>

<para>
Information concerning webcams can be located on the Ubuntu community
documentation site at <ulink url="https://help.ubuntu.com/community/Webcam"/>.
</para>

</sect1>

<sect1 id="harddrives" status="complete">
<title>Hard drives - Partitioning and Booting</title>

<sect2 id="partition-editor" status="complete">
<title>Graphical Partition Editor</title>

<procedure>
<step>
<para>
Install the <application>qtparted</application> package (refer to the <ulink
type="help" url="help:/kubuntu/add-applications/">Adding Applications</ulink>
documentation for more help on installing applications).
</para>
</step>

<step>
<para>
Once installed, open <application>QtParted</application> by going to
&menuqtparted;.
</para>
</step>
</procedure>
</sect2>

<sect2 id="listpartitiontables" status="complete">
<title>Check disk space usage and view the partition table</title>

<procedure>
<step>
<para>
Open <application>System Settings</application> by going to &menusysset;.
Select the <guilabel>Advanced</guilabel> tab and then select <guilabel>Disks
&amp; Filesystems</guilabel>.
</para>
</step>

<step>
<para>
Each partition will be listed under <guilabel>Available Disks and
Filesystems</guilabel> details of each partition.
</para>
</step>
</procedure>
</sect2>

<sect2 id="listmounteddevices" status="complete">
<title>List mounted devices in a terminal</title>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
To list the mounted devices type the following and then press the
<keycap>Enter</keycap> key:
<screen>mount</screen>
</para>
</step>
</procedure>

<para>
The listing shows the device (such as a hard disk partition), the mount point
(where you access the files), the filesystem type and the mount options.
</para>

<para>
This example shows the hda2 hard disk partition mounted as '/', with the
filesystem type ext3. The partition is mounted with two options, one to allow
the device to be read from and wrote to and the other to remount the device
as read only in the event of any errors.
</para>

<screen>
<computeroutput>
/dev/hda2 on / type ext3 (rw,errors=remount-ro)
</computeroutput>
</screen>
</sect2>

<sect2 id="mount-unmount-windows-partitions" status="complete">
<title>Mount/Unmount Windows partitions</title>

<note><title>NTFS or FAT32</title>
<para>
For <acronym>NTFS</acronym> drives refer to step 4. For
<acronym>FAT32</acronym> drives refer to step 5. <acronym>NTFS</acronym> allows
only <emphasis>read-only</emphasis> access whereas <acronym>FAT32</acronym>
allows both <emphasis>read</emphasis> and <emphasis>write</emphasis> access.
</para>
</note>

<procedure>
<step>
<para>
Read <xref linkend="listpartitiontables"/>
</para>
</step>

<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
First, make a directory where the partition can be mounted. To do so, type the
following at the command prompt followed by pressing the <keycap>Enter</keycap>
key (if asked for a password, enter your user password and press the
<keycap>Enter</keycap> key):
<screen>
sudo mkdir /media/windows
</screen>
</para>
</step>

<step>
<para>
Next, type the following command to mount an NTFS Windows Partition and allow
read-only access followed by pressing the <keycap>Enter</keycap> key (if asked
for a password, enter your user password and press the <keycap>Enter</keycap>
key):
<screen>
sudo mount /dev/hdaX /media/windows/ -t ntfs -o ro,umask=0222
</screen>
</para>
</step>

<step>
<para>
If your Windows partition uses the FAT32 filesystem, it is safe to allow
read-write access to the partition. To do so, type the following command at the
prompt followed by the <keycap>Enter</keycap> key (if asked for a password,
enter your user password and press the <keycap>Enter</keycap> key):
<screen>
sudo mount /dev/hdaX /media/windows/ -t vfat -o umask=0000
</screen>
</para>
<note>
<para>
Replace <emphasis role="bold">/dev/hdaX</emphasis> with the correct device name
for your partition.
</para>
</note>
</step>

<step>
<para>
To unmount the partition, type the following command at the prompt followed by
the <keycap>Enter</keycap> key (if asked for a password, enter your user
password and press the <keycap>Enter</keycap> key):
<screen>
sudo umount /media/windows/
</screen>
</para>
</step>
</procedure>
</sect2>

<sect2 id="mount-unmount-at-bootup" status="complete">
<title>Mount Windows partitions on boot-up</title>

<procedure>
<step>
<para>Read <xref linkend="listpartitiontables"/>
</para>
</step>

<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
First, make a directory where the partition can be mounted. To do so, type the
following at the command prompt followed by pressing the <keycap>Enter</keycap>
key (if asked for a password, enter your user password and press the
<keycap>Enter</keycap> key):
<screen>
sudo mkdir /media/windows
</screen>
</para>
</step>

<step>
<para>
Next, backup your configuration file for mounting drives and open the file in a
text editor with administrative privileges. To do so, type the following at the
prompt followed by pressing the <keycap>Enter</keycap> key (if asked for a
password, enter your user password and press the <keycap>Enter</keycap> key):
<screen>
sudo cp /etc/fstab /etc/fstab_backup
</screen>
</para>
</step>

<step>
<para>
Edit your <filename>/etc/fstab</filename> by pressing
<keycombo><keycap>Alt</keycap><keycap>F2</keycap></keycombo> and then typing
<userinput>kdesu kate /etc/fstab</userinput> in the <guilabel>Run
Command</guilabel> dialog followed by pressing the <guibutton>OK</guibutton>
button.
</para>
</step>

<step>
<para>
If your Windows partition uses the NTFS filesystem, append the following line at
the end of file:
<programlisting>
/dev/hdaX /media/windows ntfs ro,umask=0222 0 0
</programlisting>
</para>

<para>
If your Windows partition uses the FAT32 filesystem, it is safe to allow
read-write access to the partition. Append the following line at the end of the
file:
<programlisting>
/dev/hdaX /media/windows vfat umask=0000 0 0</programlisting>
instead of the one above.
</para>
<note>
<para>
Replace <emphasis role="bold">/dev/hdaX</emphasis> with the correct device name
for your partition.
</para>
</note>
</step>

<step>
<para>
Save and close out of the edited file.
</para>
</step>

<step>
<para>
Read <xref linkend="remountfstabwithoutreboot"/>.
</para>
</step>
</procedure>
</sect2>

<sect2 id="remountfstabwithoutreboot" status="complete">
<title>Remount /etc/fstab without rebooting</title>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
At the prompt, type the following and then press the <keycap>Enter</keycap> key
(if asked for a password, enter your user password and press the
<keycap>Enter</keycap> key):
<screen>
sudo mount -a
</screen>
</para>
</step>
</procedure>
</sect2>

<sect2 id="run-on-bootup" status="complete">
<title>Run a system command automatically at Startup</title>

<para>
Sometimes it can be useful to add a custom command to the startup process so
that your computer executes it on every boot. Refer to the following procedure
in order to set this up.
</para>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
Edit the <application>crontab</application> with administrative privileges. To
do so, type the following at the command prompt followed by the
<keycap>Enter</keycap> key (if asked for a password, enter your user password
and press the <keycap>Enter</keycap> key):
<screen>
sudo crontab -e
</screen>
</para>
</step>

<step>
<para>
Insert the following line:
<screen>
@reboot /home/user/command
</screen>
<note>
<para>
Replace <filename>/home/user/command</filename> with the full path to your
command.
</para>
</note>
</para>
</step>

<step>
<para>
Save the file and exit. If using <application>Nano</application> as the default
command line editor, press
<keycombo><keycap>Ctrl</keycap><keycap>X</keycap></keycombo> followed by
<keycap>Y</keycap> for <emphasis>Yes</emphasis> and then the
<keycap>Enter</keycap> key.
</para>
</step>
</procedure>
</sect2>

<sect2 id="change-default-os" status="complete">
<title>Change default Operating System at boot</title>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
Make a backup of your boot configuration file. To do so, type the following at
the command prompt followed by the <keycap>Enter</keycap> key (if asked for a
password, enter your user password and press the <keycap>Enter</keycap> key):
<screen>
sudo cp /boot/grub/menu.lst /boot/grub/menu.lst_backup
</screen>
</para>
</step>

<step>
<para>
Edit the <filename>menu.lst</filename> file by pressing
<keycombo><keycap>Alt</keycap><keycap>F2</keycap></keycombo> and then typing
<userinput>kdesu kwrite /boot/grub/menu.lst</userinput> followed by pressing
the <guibutton>OK</guibutton> button.
</para>
</step>

<step>
<para>Locate the following line:
<programlisting>
...
default 0
...
</programlisting>
and replace it with the following line:
<programlisting>default X_sequence</programlisting>
</para>
</step>

<step>
<para>
Save and close out of the edited file.
</para>
</step>
</procedure>
</sect2>

<sect2 id="boot-menu-visible" status="complete">
<title>Making the Boot Menu Visible</title>

<para>
By default, the boot menu is hidden and you will need to hit
<keycap>Esc</keycap> to see the boot menu. By following the following steps, it
will be visible by default.
</para>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
Make a backup of your boot configuration file. To do so, type the following at
the command prompt followed by the <keycap>Enter</keycap> key (if asked for a
password, enter your user password and press the <keycap>Enter</keycap> key):
<screen>
sudo cp /boot/grub/menu.lst /boot/grub/menu.lst_backup
</screen>
</para>
</step>

<step>
<para>
Edit the <filename>menu.lst</filename> file by pressing
<keycombo><keycap>Alt</keycap><keycap>F2</keycap></keycombo> and then typing
<userinput>kdesu kwrite /boot/grub/menu.lst</userinput> in the <guilabel>Run
Command</guilabel> dialog followed by pressing the <guibutton>OK</guibutton>
button.
</para>
</step>

<step>
<para>
Locate the following line:
<programlisting>
# hiddenmenu
</programlisting>
and uncomment it by removing the <emphasis>#</emphasis> in front of the line so
that it looks like:
<programlisting>
hiddenmenu
</programlisting>
</para>
</step>

<step>
<para>
Save and close out of the edited file.
</para>
</step>
</procedure>
</sect2>

<sect2 id="change-timeout" status="complete">
<title>Change the timeout for boot menu</title>

<procedure>
<step>
<para>
Open <application>Konsole</application> by going to &menukonsole;.
</para>
</step>

<step>
<para>
Make a backup of your boot configuration file. To do so, type the following at
the command prompt followed by the <keycap>Enter</keycap> key (if asked for a
password, enter your user password and press the <keycap>Enter</keycap> key):
<screen>
sudo cp /boot/grub/menu.lst /boot/grub/menu.lst_backup
</screen>
</para>
</step>

<step>
<para>
Edit the <filename>menu.lst</filename> file by pressing
<keycombo><keycap>Alt</keycap><keycap>F2</keycap></keycombo> and then typing
<userinput>kdesu kwrite /boot/grub/menu.lst</userinput> in the <guilabel>Run
Command</guilabel> dialog followed by pressing the <guibutton>OK</guibutton>
button.
</para>
</step>

<step>
<para>
Locate the following line:
<programlisting>
...
timeout 3
...
</programlisting>
and replace it with the following line:
<programlisting>
timeout X_seconds
</programlisting>
</para>
</step>

<step>
<para>
Save and close out of the edited file.
</para>
</step>
</procedure>
</sect2>

</sect1>

<!-- KEYBOARD & MOUSE -->
<sect1 id="mouse" status="complete">
<title>Keyboard &amp; Mouse</title>

<para>
With the mass amount of various keyboards, mice, and keyboard and mouse combos,
your options in Kubuntu are near endless. Whether you use
<acronym>QWERTY</acronym> or Dvorak, Logitech or Microsoft, 5-button or
7-button, there are options for you. Due to the mass amount of options, more
information on this subject is available on the Ubuntu community documentation
site at <ulink url="https://help.ubuntu.com/community/Peripherals"/>.
</para>

</sect1>

<!-- MODEMS -->
<sect1 id="modems" status="complete">
<title>Modems</title>

<para>
Please review the <ulink type="help" url="help:/kubuntu/network/">Networking
Documentation</ulink> for modem hardware.
</para>

</sect1>

<!-- NETWORK -->
<sect1 id="network" status="complete">
<title>Network Cards</title>

<para>
Please review the <ulink type="help" url="help:/kubuntu/network/">Networking
Documentation</ulink> for Ethernet and Wireless hardware.
</para>

</sect1>

<!-- PRINTERS -->
<sect1 id="printers" status="complete">
<title>Printers</title>

<para>
Please review the <ulink type="help" url="help:/kubuntu/printing/">Printing
Documentation</ulink> for printers and printing.
</para>

</sect1>

<!-- SCANNERS -->
<sect1 id="scanners" status="complete">
<title>Scanners</title>

<para>
Please review the <ulink type="help" url="help:/kubuntu/printing/">Printing
Documentation</ulink> for scanners and scanning.
</para>

</sect1>

<!-- SOUND -->
<sect1 id="sound" status="complete">
<title>Sound</title>

<para>
Sound cards these days typically come from the more popular chipsets and a
majority of the time works out-of-the-box in Kubuntu. If you are having sound
issues it is recommended that you refer to the Ubuntu community documentation
on this subject at <ulink url="https://help.ubuntu.com/community/Sound"/>.
</para>

</sect1>

<!-- VIDEO -->
<sect1 id="video" status="complete">
<title>Video Cards</title>

<para>
<acronym>ATI</acronym>, Nvidia, Intel, Matrox, or even those of you using
Voodoo graphics cards are going to have either some issues or no issues at all.
A majority of these cards may not offer rendering support without the use of
what is known as a binary or restricted driver. <acronym>ATI</acronym> and
Nvidia both require binary drivers for accelerated rendering support. Integrated
Intel cards, when paired with a wide screen display normally need an extra
utility in order to achieve proper resolutions and refresh rates. These options
will be explained further in the following subsections.
</para>

<para>
Further information for video cards can be located on the Ubuntu community
documentation site at <ulink url="https://help.ubuntu.com/community/Video"/>.
</para>

<sect2 id="ati" status="complete">
<title>ATI Radeon Video Cards</title>

<para>
Included with your new Kubuntu system is the <application>Restricted
Manager</application> which allows you to easily install the <productname>ATI
Radeon</productname> binary or restricted drivers.
</para>

<procedure><title>ATI Radeon restricted driver installation</title>
<step>
<para>
Open <application>System Settings</application> by going to &menusysset;.
</para>
</step>
<step>
<para>
Once in <application>System Settings</application>
<mousebutton>click</mousebutton> the <guilabel>Advanced</guilabel> tab at the
top.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guiicon>Restricted Drivers</guiicon> icon.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guibutton>Administrator
Mode...</guibutton> button. When prompted for a password, enter your user
password followed by <mousebutton>clicking</mousebutton> the
<guibutton>OK</guibutton> button.
</para>
</step>
<step>
<para>
Select the <guilabel>ATI accelerated graphics driver</guilabel> if it has the
<guilabel>Not in use</guilabel> <guilabel>Status</guilabel>.
</para>
</step>
<step>
<para>
When prompted with the <guilabel>Enable the Driver? - KDE Control
Module</guilabel> dialog, <mousebutton>click</mousebutton> the
<guibutton>Enable Driver</guibutton> button.
</para>
</step>
<step>
<para>
<application>Adept</application> will launch to install the driver. When the
driver installation is complete, you should notice the <guilabel>Needs computer
restart</guilabel> <guilabel>Status</guilabel>.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guibutton>Close</guibutton> button.
</para>
</step>
<step>
<para>
Reboot your computer by going to &menulogout; and
<mousebutton>clicking</mousebutton> the <guiicon>Restart</guiicon> icon.
</para>
</step>
</procedure>

</sect2>

<sect2 id="nvidia" status="complete">
<title>NVIDIA GeForce Video Cards</title>

<para>
Included with your new Kubuntu system is the <application>Restricted
Manager</application> which allows you to easily install the
<productname>NVIDIA GeForce</productname> binary or restricted drivers.
</para>

<procedure><title>NVIDIA GeForce restricted driver installation</title>
<step>
<para>
Open <application>System Settings</application> by going to &menusysset;.
</para>
</step>
<step>
<para>
Once in <application>System Settings</application>
<mousebutton>click</mousebutton> the <guilabel>Advanced</guilabel> tab at the
top.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guiicon>Restricted Drivers</guiicon> icon.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guibutton>Administrator
Mode...</guibutton> button. When prompted for a password, enter your user
password followed by <mousebutton>clicking</mousebutton> the
<guibutton>OK</guibutton> button.
</para>
</step>
<step>
<para>
Select the <guilabel>NVIDIA accelerated graphics driver</guilabel> if it has the
<guilabel>Not in use</guilabel> <guilabel>Status</guilabel>.
</para>
</step>
<step>
<para>
When prompted with the <guilabel>Enable the Driver? - KDE Control
Module</guilabel> dialog, <mousebutton>click</mousebutton> the
<guibutton>Enable Driver</guibutton> button.
</para>
</step>
<step>
<para>
<application>Adept</application> will launch to install the driver. When the
driver installation is complete, you should notice the <guilabel>Needs computer
restart</guilabel> <guilabel>Status</guilabel>.
</para>
</step>
<step>
<para>
<mousebutton>Click</mousebutton> the <guibutton>Close</guibutton> button.
</para>
</step>
<step>
<para>
Reboot your computer by going to &menulogout; and
<mousebutton>clicking</mousebutton> the <guiicon>Restart</guiicon> icon.
</para>
</step>
</procedure>

</sect2>
<sect2 id="intel" status="complete">
<title>Intel Video Cards</title>

<para>
Some Intel video cards will require an application in order to achieve the
correct resolution and refresh settings. The
<application>915resolution</application> application will fix any resolution
issues you may have after installing Kubuntu. Simply install the
<application>915resolution</application> package (see the <ulink
url="help:/kubuntu/add-applications/">Adding Applications</ulink> documentation
for installation instructions) and then restart the Kubuntu desktop by pressing
&ctrlaltbkspc;. Your resolution should now be properly configured.
</para>

</sect2>

</sect1>


</article>
